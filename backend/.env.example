# Environment variables for Crop Prediction API
DEBUG=True
LOG_LEVEL=INFO

# Model settings
MODEL_PATH=saved_models/crop_predictor.joblib
MODEL_TYPE=random_forest

# Random Forest parameters
RF_N_ESTIMATORS=100
RF_MAX_DEPTH=
RF_MIN_SAMPLES_SPLIT=2
RF_MIN_SAMPLES_LEAF=1
RF_RANDOM_STATE=42

# Database settings (configure these to connect to your friend's database)
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crop_prediction
DB_USER=your_username
DB_PASSWORD=your_password

# Data processing
TEST_SIZE=0.2
VALIDATION_SPLIT=0.2